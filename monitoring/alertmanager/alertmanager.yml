route:
  receiver: default
  group_by:
    - alertname
    - severity
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: default
    webhook_configs:
      - url: http://alert-webhook:8080
        send_resolved: true
